import { FileSpreadsheet } from "lucide-react";
import { AuditShell } from "@/components/AuditShell";
import { MetricLineChart, formatK, formatPct } from "@/components/AuditCharts";
import { useAudit } from "@/contexts/AuditContext";
import { expenseDefinitions, useAuditFacts } from "@/hooks/useAuditFacts";

const money=(value:number)=>formatK(value/1000);
const median=(values:number[])=>{const sorted=[...values].sort((a,b)=>a-b);const middle=Math.floor(sorted.length/2);return sorted.length?sorted.length%2?sorted[middle]:(sorted[middle-1]+sorted[middle])/2:0};

export default function Stores(){const facts=useAuditFacts();const {selectedStore,setSelectedStore,rangeLabel}=useAudit();const storeName=selectedStore!=="__all__"&&facts.storeNames.includes(selectedStore)?selectedStore:facts.storeNames[0];const store=facts.summaries.find(summary=>summary.store===storeName)??facts.network;const rank=[...facts.summaries].sort((a,b)=>b.netProfit-a.netProfit).findIndex(summary=>summary.store===storeName)+1;const monthly=facts.monthlyFor(storeName).map(row=>({month:row.month,revenue:row.revenue/1000,grossProfit:row.grossProfit/1000,netProfit:row.netProfit/1000}));const expenses=expenseDefinitions.map(([code,label])=>{const amount=Math.abs(store.expenseByCode[code]??0),share=store.revenue?amount/store.revenue*100:0,medianShare=median(facts.summaries.map(summary=>summary.revenue?Math.abs(summary.expenseByCode[code]??0)/summary.revenue*100:0));return {code,label,amount,share,medianShare,gap:share-medianShare}}).sort((a,b)=>b.amount-a.amount);const smokedMarkup=store.purchaseSmoked?((store.salesSmoked/store.purchaseSmoked)-1)*100:0;const frozenMarkup=store.purchaseFrozen?((store.salesFrozen/store.purchaseFrozen)-1)*100:0;return <AuditShell kicker="05 / ПРОФИЛИ ТОЧЕК" title="Магазин: полный разбор">{facts.loading?<section className="empty-state"><p>Загружаем факты…</p></section>:!facts.available?<section className="empty-state live-empty"><FileSpreadsheet size={30}/><h2>Нет магазина для разбора</h2><p>После импорта Excel в профиле появится детальная аналитика выбранного магазина.</p></section>:<><section className="page-lede"><div><h2>Одна точка — весь P&L, товар, остаток и расходы.</h2><p>Диапазон: <b>{rangeLabel}</b>. Ранги и медианы рассчитываются по доступным магазинам и импортированным фактам.</p></div><div className="page-controls"><label>Магазин<select value={storeName} onChange={event=>setSelectedStore(event.target.value)}>{facts.storeNames.map(name=>{const item=facts.summaries.find(summary=>summary.store===name);const itemRank=[...facts.summaries].sort((a,b)=>b.netProfit-a.netProfit).findIndex(summary=>summary.store===name)+1;return <option key={name} value={name}>{name} · #{itemRank} по прибыли {item&&item.netProfit<0?"· риск":""}</option>})}</select></label></div></section><section className="packet-kpis equal"><article className="packet-kpi"><span>Выручка</span><strong>{money(store.revenue)}</strong><small>валовая маржа {formatPct(store.grossMargin)}</small></article><article className={store.netProfit<0?"packet-kpi risk":"packet-kpi"}><span>Чистая прибыль</span><strong>{money(store.netProfit)}</strong><small>маржа {formatPct(store.netMargin)} · ранг #{rank}</small></article><article className="packet-kpi"><span>Все расходы</span><strong>{money(store.expenses)}</strong><small>фактический P&L</small></article><article className="packet-kpi"><span>Конечный остаток</span><strong>{money(store.stockClose)}</strong><small>{store.coverDays.toFixed(1)} дня покрытия</small></article></section><section className="packet-split"><article className="packet-card wide"><div className="card-title"><div><span>P&L · {storeName}</span><h3>Выручка, валовая и чистая прибыль</h3></div><small>млн / тыс. ₽</small></div><MetricLineChart data={monthly} lines={[{key:"revenue",name:"Выручка",color:"#b694ff"},{key:"grossProfit",name:"Валовая прибыль",color:"#ffcf7b"},{key:"netProfit",name:"Чистая прибыль",color:"#ff6d8c"}]}/></article><article className="packet-card"><div className="card-title"><div><span>ТОВАРНЫЕ КАТЕГОРИИ</span><h3>Коп. и Мор. без приоритета</h3></div></div><div className="product-ledger"><div><span>Коп. · закупка / продажа</span><strong>{money(store.purchaseSmoked)} / {money(store.salesSmoked)}</strong><small>наценка {formatPct(smokedMarkup)}</small></div><div><span>Мор. · закупка / продажа</span><strong>{money(store.purchaseFrozen)} / {money(store.salesFrozen)}</strong><small>наценка {formatPct(frozenMarkup)}</small></div></div><div className="product-ledger"><div><span>Списания К.</span><strong>{money(store.writeoffSmoked)}</strong><small>{formatPct(store.revenue?store.writeoffSmoked/store.revenue*100:0)} выручки</small></div><div><span>Списания М.</span><strong className="negative">{money(store.writeoffFrozen)}</strong><small>{formatPct(store.revenue?store.writeoffFrozen/store.revenue*100:0)} выручки · P&L расход</small></div></div></article></section><section className="packet-card"><div className="card-title"><div><span>ВСЕ РАСХОДЫ · {storeName}</span><h3>Сумма, доля выручки, сетевая медиана</h3></div></div><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Статья</th><th>Сумма</th><th>Доля</th><th>Медиана</th><th>Отклонение</th></tr></thead><tbody>{expenses.map(row=><tr key={row.code}><td>{row.label}</td><td>{money(row.amount)}</td><td>{formatPct(row.share)}</td><td>{formatPct(row.medianShare)}</td><td className={row.gap>0?"negative":"positive"}>{row.gap>0?"+":""}{formatPct(row.gap)}</td></tr>)}</tbody></table></div></section></>}</AuditShell>}
