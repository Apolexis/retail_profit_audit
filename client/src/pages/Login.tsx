import { useState } from "react";
import { LockKeyhole, Sparkles } from "lucide-react";
import { trpc } from "@/lib/trpc";

export default function Login(){const [username,setUsername]=useState("");const [password,setPassword]=useState("");const utils=trpc.useUtils();const login=trpc.localAuth.login.useMutation({onSuccess:()=>utils.localAuth.me.invalidate()});const submit=(event:React.FormEvent)=>{event.preventDefault();login.mutate({username,password})};return <main className="login-gate"><section className="login-panel"><div className="login-mark"><Sparkles size={22}/><span>AuditLine / закрытая аналитика</span></div><h1>Вход в операционный контур</h1><p>Данные сети, импорт Excel и управление фактами доступны только авторизованным пользователям.</p><form onSubmit={submit}><label>Логин<input autoComplete="username" value={username} onChange={event=>setUsername(event.target.value)} required/></label><label>Пароль<input type="password" autoComplete="current-password" value={password} onChange={event=>setPassword(event.target.value)} required/></label>{login.error&&<div className="login-error">{login.error.message}</div>}<button className="login-submit" disabled={login.isPending}><LockKeyhole size={16}/>{login.isPending?"Проверяем доступ…":"Войти"}</button></form><small>После входа можно изменить пароль в профиле. Действия с данными записываются в журнал изменений.</small></section></main>}
