import { useMemo, useRef, useState } from "react";
import { AlertTriangle, ArrowRight, CheckCircle2, FileSpreadsheet, History, LoaderCircle, Merge, ShieldAlert, Trash2, UploadCloud } from "lucide-react";
import { Link } from "wouter";
import { AuditShell } from "@/components/AuditShell";
import { ConfirmDangerDialog } from "@/components/ConfirmDangerDialog";
import { DateRangeControl } from "@/components/DateRangeControl";
import { importStatusMeta, isImportBusy, type ImportStage } from "@/lib/importStatus";
import { trpc } from "@/lib/trpc";
import type { DateRangeValue } from "@/contexts/AuditContext";

type Uploaded={fileName:string;fileBase64:string};
const asBase64=(file:File)=>new Promise<Uploaded>((resolve,reject)=>{const reader=new FileReader();reader.onerror=()=>reject(new Error("Не удалось прочитать файл"));reader.onload=()=>resolve({fileName:file.name,fileBase64:String(reader.result).split(",")[1]??""});reader.readAsDataURL(file)});
const dateTime=(value:Date|string)=>new Date(value).toLocaleString("ru-RU",{dateStyle:"medium",timeStyle:"short"});
const fullYearFor=(fileName:string):DateRangeValue=>{const year=Number(fileName.match(/(20\d{2})/)?.[1]??new Date().getFullYear());return {from:`${year}-01-01`,to:`${year}-12-31`}};

export default function ImportData(){
  const input=useRef<HTMLInputElement>(null);const utils=trpc.useUtils();const me=trpc.localAuth.me.useQuery(undefined,{retry:false});const imports=trpc.audit.imports.useQuery(undefined,{retry:false});const stores=trpc.audit.stores.useQuery(undefined,{retry:false});
  const [payload,setPayload]=useState<Uploaded|null>(null),[drag,setDrag]=useState(false),[resolution,setResolution]=useState<"replace"|"skip"|"preserve_manual">("preserve_manual"),[message,setMessage]=useState(""),[stage,setStage]=useState<ImportStage>("idle"),[sourceStoreId,setSourceStoreId]=useState<number>(),[targetStoreId,setTargetStoreId]=useState<number>(),[mergeResolution,setMergeResolution]=useState<"replace"|"skip">("replace"),[importRange,setImportRange]=useState<DateRangeValue>(()=>fullYearFor(""));
  const isAdmin=me.data?.role==="admin";const preview=trpc.audit.previewImport.useMutation({onSuccess:()=>setStage("ready"),onError:error=>{setStage("error");setMessage(error.message)}});const commit=trpc.audit.commitImport.useMutation({onMutate:()=>{setStage("saving");setMessage("")},onSuccess:data=>{setStage("done");setMessage(`Импорт завершен: новых магазинов — ${data.createdStores}, новых периодов — ${data.createdPeriods}, обновлено дат — ${data.updatedPeriods}${data.protectedMetricCount?`, сохранено ручных правок — ${data.protectedMetricCount}`:""}.`);utils.audit.dashboard.invalidate();utils.audit.stores.invalidate();utils.audit.imports.invalidate()},onError:error=>{setStage("error");setMessage(error.message)}});const deleteImport=trpc.audit.deleteImport.useMutation({onSuccess:data=>{setMessage(`Импорт «${data.fileName}» удален: периодов — ${data.deletedPeriods}, пустых магазинов — ${data.deletedStores}.`);utils.audit.dashboard.invalidate();utils.audit.stores.invalidate();utils.audit.imports.invalidate()},onError:error=>setMessage(error.message)});const mergeStores=trpc.audit.mergeStores.useMutation({onSuccess:data=>{setMessage(`Объединение выполнено: перенесено — ${data.moved}, заменено совпадений — ${data.replaced}, пропущено — ${data.skipped}.`);setSourceStoreId(undefined);utils.audit.dashboard.invalidate();utils.audit.stores.invalidate();utils.audit.imports.invalidate()},onError:error=>setMessage(error.message)});
  const accept=async(file?:File)=>{if(!file)return;if(!/\.(xlsx|xlsm)$/i.test(file.name)){setStage("error");setMessage("Поддерживается книга Excel .xlsx или .xlsm.");return}try{setStage("reading");const next=await asBase64(file);setPayload(next);setImportRange(fullYearFor(file.name));setMessage("");setStage("checking");preview.mutate(next)}catch(error){setStage("error");setMessage(error instanceof Error?error.message:"Ошибка чтения файла")}};
  const storeOptions=stores.data??[];const canMerge=Boolean(sourceStoreId&&targetStoreId&&sourceStoreId!==targetStoreId);const chosenSource=storeOptions.find(store=>store.id===sourceStoreId);const chosenTarget=storeOptions.find(store=>store.id===targetStoreId);const importRows=useMemo(()=>imports.data??[],[imports.data]);const hasFacts=importRows.some(item=>item.periods>0);const isInImportRange=(entryDate:string)=>entryDate>=importRange.from&&entryDate<=importRange.to;const selectedPreviewPeriods=useMemo(()=>preview.data?.periods.filter(item=>isInImportRange(item.entryDate))??[],[preview.data,importRange.from,importRange.to]);const selectedConflicts=useMemo(()=>preview.data?.conflicts.filter(item=>isInImportRange(item.entryDate))??[],[preview.data,importRange.from,importRange.to]);const selectedProtected=useMemo(()=>preview.data?.protectedMetrics.filter(item=>isInImportRange(item.entryDate))??[],[preview.data,importRange.from,importRange.to]);
  const submitImport=()=>payload&&commit.mutate({...payload,resolution,dateRange:importRange});const needsReplacementConfirm=Boolean(selectedConflicts.length&&resolution!=="skip");const stageKeys=Object.keys(importStatusMeta) as ImportStage[];const stageText=Object.fromEntries(stageKeys.map(key=>[key,importStatusMeta[key].label])) as Record<ImportStage,string>;const stageSteps=Object.fromEntries(stageKeys.map(key=>[key,importStatusMeta[key].step])) as Record<ImportStage,number>;const stageStep=stageSteps[stage];
  if(!isAdmin)return <AuditShell kicker="10 / ИМПОРТ" title="Импорт Excel: обновить факты"><section className="empty-state"><ShieldAlert size={30}/><h2>Импорт доступен администратору</h2><p>Аналитик работает с назначенными магазинами. Чтобы загрузить или удалить книгу Excel, войдите под учетной записью администратора.</p></section></AuditShell>;
  return <AuditShell kicker="10 / ИМПОРТ И БАЗА" title="Импорт Excel: обновить факты">
    <section className="page-lede"><div><h2>Загрузите обновленную книгу без отправки в чат.</h2><p>Файл сохраняется в защищенном хранилище, а показатели нормализуются в базу. Перед записью система покажет новые магазины, новые даты и совпадения с уже сохраненными фактами.</p></div><Link href="/manage" className="packet-link compact">Открыть управление базой <ArrowRight size={15}/></Link></section>
    {!hasFacts&&<section className="data-source-banner"><FileSpreadsheet size={19}/><div><strong>В базе пока нет импортированных фактов.</strong><span>Данные аналитики появятся после первого подтвержденного импорта.</span></div></section>}
    <section className="import-layout"><article className="packet-card import-drop"><input ref={input} type="file" accept=".xlsx,.xlsm,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/vnd.ms-excel.sheet.macroEnabled.12" hidden onChange={event=>accept(event.target.files?.[0])}/><button className={drag?"drop-target dragging":"drop-target"} onClick={()=>input.current?.click()} onDragOver={event=>{event.preventDefault();setDrag(true)}} onDragLeave={()=>setDrag(false)} onDrop={event=>{event.preventDefault();setDrag(false);accept(event.dataTransfer.files?.[0])}}><UploadCloud size={32}/><b>Перетащите книгу Excel сюда</b><span>или выберите файл с компьютера</span><small>Поддерживаются .xlsx и .xlsm</small></button><div className={`upload-status stage-${stage}`} aria-live="polite"><div><span className={stageStep>=1?"done":""}>1</span><p><b>Файл</b><small>{stage==="reading"?"читаем из браузера":payload?payload.fileName:"ожидание"}</small></p></div><div><span className={stageStep>=2?"done":""}>2</span><p><b>Проверка</b><small>{stage==="checking"?"распознаем листы и даты":stageStep>=2?"предпросмотр готов":"после выбора файла"}</small></p></div><div><span className={stageStep>=3?"done":""}>3</span><p><b>Запись</b><small>{stage==="saving"?"сохраняем выбранные даты":stage==="done"?"готово":"после подтверждения"}</small></p></div><strong>{["reading","checking","saving"].includes(stage)&&<LoaderCircle size={14} className="upload-spin"/>}{stageText[stage]}</strong></div>{payload&&<div className="upload-file"><FileSpreadsheet size={18}/><span>{payload.fileName}</span><button onClick={()=>{setPayload(null);preview.reset();setMessage("");setStage("idle")}}>Убрать файл</button></div>}{message&&<p className={message.startsWith("Импорт завершен")?"import-message success":"import-message"}>{message}</p>}</article><article className="packet-card import-requirements"><div className="card-title"><div><span>ТРЕБОВАНИЯ К ФАЙЛУ</span><h3>Как система читает книгу</h3></div></div><div className="requirement-list"><div><b>1</b><p><strong>Год берется из имени файла.</strong> Например, <code>2026.xlsx</code> создает даты 2026 года.</p></div><div><b>2</b><p><strong>Первые три листа пропускаются.</strong> Они считаются техническими или сводными.</p></div><div><b>3</b><p><strong>Каждый лист начиная с 4-го — отдельный магазин.</strong> Переименованный лист создаст новую точку; при необходимости ее можно объединить с существующей ниже.</p></div><div><b>4</b><p><strong>Книга читается по дням.</strong> Продажи, закупки, списания и прочие ежедневные строки остаются на своей фактической дате.</p></div><div><b>5</b><p><strong>Ежемесячные статьи разнесены только в «Ритме».</strong> Указанные расходы и итоговая чистая прибыль, записанные раз в месяц, распределяются по календарным дням только для недельно-дневного среза; исходные факты в базе не меняются.</p></div><div><b>6</b><p><strong>Совпадение «магазин + дата» — конфликт.</strong> Замените сохраненные факты данными файла либо пропустите совпадения.</p></div></div></article></section>
    {preview.data&&<section className="packet-card"><div className="card-title"><div><span>ПРЕДПРОСМОТР ИМПОРТА</span><h3>Файл распознан: {preview.data.year} год</h3></div><small>{selectedPreviewPeriods.length} магазин-дата к импорту</small></div><div className="import-date-filter"><div><span>ДАТЫ ДЛЯ ЗАГРУЗКИ</span><strong>Выберите календарный диапазон</strong><small>В базу попадут только распознанные даты файла внутри интервала. Исходная книга при этом не меняется.</small></div><DateRangeControl value={importRange} onChange={setImportRange} title="ДАТЫ ДЛЯ ИМПОРТА" ariaLabel="Выбрать даты импорта"/></div><div className="import-summary"><div><span>Магазины в файле</span><strong>{preview.data.stores.length}</strong><small>{preview.data.stores.join(", ")}</small></div><div><span>Даты в выбранном диапазоне</span><strong>{selectedPreviewPeriods.length}</strong><small>{importRange.from} — {importRange.to}</small></div><div className={selectedConflicts.length?"risk":""}><span>Совпадения дат</span><strong>{selectedConflicts.length}</strong><small>{selectedConflicts.length?"нужно выбрать действие":"конфликтов в диапазоне нет"}</small></div><div className={selectedProtected.length?"warning":""}><span>Ручные правки</span><strong>{selectedProtected.length}</strong><small>{selectedProtected.length?"можно сохранить при обновлении":"защищать нечего"}</small></div></div>{selectedConflicts.length>0&&<><div className="conflict-box"><AlertTriangle size={18}/><div><b>В выбранном диапазоне есть сохраненные даты</b><span>{selectedConflicts.slice(0,8).map(item=>`${item.store} · ${item.entryDate??item.monthDate}`).join("; ")}{selectedConflicts.length>8?" …":""}</span></div></div><div className="conflict-actions"><label><input type="radio" checked={resolution==="preserve_manual"} onChange={()=>setResolution("preserve_manual")}/> Обновить импортные данные, сохранить ручные правки</label><label><input type="radio" checked={resolution==="replace"} onChange={()=>setResolution("replace")}/> Полностью заменить сохраненные значения данными файла</label><label><input type="radio" checked={resolution==="skip"} onChange={()=>setResolution("skip")}/> Пропустить совпадающие даты</label><Link href="/manage">Открыть точечное редактирование</Link></div></>}{needsReplacementConfirm?<ConfirmDangerDialog trigger={<button className="packet-link" disabled={commit.isPending||!selectedPreviewPeriods.length}><CheckCircle2 size={15}/>{commit.isPending?"Сохраняем…":resolution==="preserve_manual"?"Подтвердить обновление":"Подтвердить замену"}</button>} title={resolution==="preserve_manual"?"Обновить данные, сохранив ручные правки?":"Заменить совпадающие факты?"} description={resolution==="preserve_manual"?`В диапазоне ${importRange.from} — ${importRange.to} будут обновлены импортные значения. ${selectedProtected.length} вручную отредактированных показателей останутся как есть.`:`Будут заменены ${selectedConflicts.length} существующих записей в диапазоне ${importRange.from} — ${importRange.to}. Новая книга останется в истории, а прежние значения можно увидеть в журнале изменений.`} confirmLabel={resolution==="preserve_manual"?"Обновить и сохранить правки":"Заменить и импортировать"} onConfirm={submitImport}/>:<button className="packet-link" disabled={commit.isPending||!payload||!selectedPreviewPeriods.length} onClick={submitImport}>{commit.isPending?"Сохраняем…":"Подтвердить импорт"}<CheckCircle2 size={15}/></button>}</section>}
    <section className="packet-card import-history"><div className="card-title"><div><span><History size={15}/> ИСТОРИЯ ИМПОРТОВ</span><h3>Какие книги сейчас формируют базу</h3></div><small>Удаление убирает из БД только факты, которые принадлежат выбранной книге.</small></div><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Книга</th><th>Год</th><th>Периоды</th><th>Магазины</th><th>Загружено</th><th/></tr></thead><tbody>{importRows.length?importRows.map(item=><tr key={item.id}><td><strong>{item.fileName}</strong></td><td>{item.sourceYear}</td><td>{item.periods}</td><td>{item.stores}</td><td>{dateTime(item.createdAt)}</td><td><ConfirmDangerDialog trigger={<button className="row-delete" disabled={deleteImport.isPending}><Trash2 size={14}/>Удалить импорт</button>} title="Удалить импорт и его факты?" description={`Будут удалены только даты, которые по-прежнему принадлежат книге «${item.fileName}». Если у магазина после этого не останется фактов, он тоже будет удален.`} onConfirm={()=>deleteImport.mutate({importId:item.id})}/></td></tr>):<tr><td colSpan={6}>Подтвержденных импортов пока нет.</td></tr>}</tbody></table></div></section>
    <section className="packet-card store-merge"><div className="card-title"><div><span><Merge size={15}/> ОБЪЕДИНЕНИЕ ТОЧЕК</span><h3>Связать переименованный лист с существующим магазином</h3></div></div><p className="packet-note">Переносит все даты и права из исходного магазина в целевой. При совпадении дат выберите, заменить ли целевые факты данными исходной точки или удалить совпадающие данные исходной точки.</p><div className="edit-controls"><label>Переименованный / исходный магазин<select value={sourceStoreId??""} onChange={event=>setSourceStoreId(Number(event.target.value)||undefined)}><option value="">Выберите магазин</option>{storeOptions.map(store=><option key={store.id} value={store.id}>{store.name}</option>)}</select></label><label>Целевой магазин<select value={targetStoreId??""} onChange={event=>setTargetStoreId(Number(event.target.value)||undefined)}><option value="">Выберите магазин</option>{storeOptions.filter(store=>store.id!==sourceStoreId).map(store=><option key={store.id} value={store.id}>{store.name}</option>)}</select></label><label>Совпадающие даты<select value={mergeResolution} onChange={event=>setMergeResolution(event.target.value as typeof mergeResolution)}><option value="replace">Заменить целевые данными исходного</option><option value="skip">Оставить целевые, убрать исходные</option></select></label></div><ConfirmDangerDialog trigger={<button className="packet-link" disabled={!canMerge||mergeStores.isPending}><Merge size={15}/>{mergeStores.isPending?"Объединяем…":`Объединить ${chosenSource?.name??""} → ${chosenTarget?.name??""}`}</button>} title="Объединить магазины?" description={`Все даты и назначенные права точки «${chosenSource?.name??""}» будут перенесены в «${chosenTarget?.name??""}». При совпадении дат действует выбранное правило: ${mergeResolution==="replace"?"целевой факт заменяется исходным":"целевой факт сохраняется, исходный удаляется"}.`} confirmLabel="Объединить точки" onConfirm={()=>sourceStoreId&&targetStoreId&&mergeStores.mutate({sourceStoreId,targetStoreId,resolution:mergeResolution})}/></section>
  </AuditShell>;
}
