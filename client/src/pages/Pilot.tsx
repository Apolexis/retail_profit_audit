import { useMemo, useState } from "react";
import { FileSpreadsheet } from "lucide-react";
import { AuditShell } from "@/components/AuditShell";
import { formatK, formatPct } from "@/components/AuditCharts";
import { useAudit } from "@/contexts/AuditContext";
import { useAuditFacts } from "@/hooks/useAuditFacts";

type Scenario={id:string;title:string;tag:string;text:string;price:number;volume:number;purchase:number;writeoff:number;opex:number;stock:number;why:string};
function Knob({label,value,onChange,min,max,step=1,suffix="%"}:{label:string;value:number;onChange:(value:number)=>void;min:number;max:number;step?:number;suffix?:string}){return <label className="pilot-knob"><span>{label}<b>{value>0?"+":""}{value}{suffix}</b></span><input type="range" min={min} max={max} step={step} value={value} onChange={event=>onChange(Number(event.target.value))}/><small>{min}{suffix} <i/> {max}{suffix}</small></label>}
const money=(value:number)=>formatK(value/1000);
const median=(values:number[])=>{const sorted=[...values].sort((a,b)=>a-b);const middle=Math.floor(sorted.length/2);return sorted.length?sorted.length%2?sorted[middle]:(sorted[middle-1]+sorted[middle])/2:0};

export default function Pilot(){const facts=useAuditFacts();const {selectedStore,setSelectedStore,rangeLabel}=useAudit();const name=selectedStore!=="__all__"&&facts.storeNames.includes(selectedStore)?selectedStore:facts.storeNames[0]??"";const store=facts.summaries.find(summary=>summary.store===name)??facts.network;const [price,setPrice]=useState(0),[volume,setVolume]=useState(0),[purchase,setPurchase]=useState(0),[writeoff,setWriteoff]=useState(0),[opex,setOpex]=useState(0),[stock,setStock]=useState(0);const sales=store.salesSmoked+store.salesFrozen,purchases=store.purchaseSmoked+store.purchaseFrozen,frozenWriteoff=store.writeoffFrozen,stockBase=store.stockClose,totalOpex=store.expenses,coverageMedian=median(facts.summaries.map(summary=>summary.coverDays));const model=useMemo(()=>{const priceProfit=sales*price/100;const volumeRevenue=sales*volume/100,volumeCost=purchases*volume/100,volumeProfit=volumeRevenue-volumeCost,purchaseProfit=-(purchases*purchase/100),writeoffProfit=frozenWriteoff*writeoff/100,opexProfit=totalOpex*opex/100,cashRelease=-(stockBase*stock/100),profit=store.netProfit+priceProfit+volumeProfit+purchaseProfit+writeoffProfit+opexProfit,revenue=store.revenue+priceProfit+volumeRevenue,cover=Math.max(0,store.coverDays*(1+stock/100));return {priceProfit,volumeProfit,purchaseProfit,writeoffProfit,opexProfit,cashRelease,profit,revenue,cover,margin:revenue?profit/revenue*100:0}},[price,volume,purchase,writeoff,opex,stock,sales,purchases,frozenWriteoff,stockBase,totalOpex,store]);const setScenario=(scenario:Scenario)=>{setPrice(scenario.price);setVolume(scenario.volume);setPurchase(scenario.purchase);setWriteoff(scenario.writeoff);setOpex(scenario.opex);setStock(scenario.stock)};const scenarios:Scenario[]=[{id:"writeoff",title:"Снизить списания М.",tag:"Быстрый контроль",text:"Проверить заказ и ротацию без изменения цены.",price:0,volume:0,purchase:0,writeoff:15,opex:0,stock:0,why:`Списания М. составляют ${money(frozenWriteoff)} за период и прямо уменьшают чистую прибыль.`},{id:"price",title:"Тест цены +1%",tag:"Контролируемый пилот",text:"Проверить одну категорию в одной точке 4 недели; консервативная реакция объема −1%.",price:1,volume:-1,purchase:0,writeoff:0,opex:0,stock:0,why:`Чистая маржа точки ${formatPct(store.netMargin)}; контролируйте чеки, количество и продажи в штуках.`},{id:"stock",title:"Высвободить избыточный запас",tag:"Оборотный капитал",text:"Снизить конечный остаток на 10% без изменения цены и контролировать out-of-stock.",price:0,volume:0,purchase:0,writeoff:0,opex:0,stock:-10,why:`Покрытие ${store.coverDays.toFixed(1)} дн. против медианы доступной сети ${coverageMedian.toFixed(1)} дн.; это эффект в деньги, а не в P&L.`},{id:"cost",title:"Операционная дисциплина",tag:"Расходы",text:"Проверить управляемые наличные и сервисные расходы, не сокращая критические смены без контроля продаж.",price:0,volume:0,purchase:0,writeoff:0,opex:5,stock:0,why:`Совокупные расходы точки: ${money(totalOpex)} за выбранный период.`},{id:"purchase",title:"Пересмотреть закупочную цену",tag:"Поставщик / ассортимент",text:"Проверить альтернативы поставщика, закупочный микс и скидки без ухудшения доступности товара.",price:0,volume:0,purchase:-2,writeoff:0,opex:0,stock:0,why:`Закупки Коп. + Мор. составляют ${money(purchases)} за выбранный период.`}];return <AuditShell kicker="08 / СЦЕНАРНЫЙ ЦЕНТР" title="Пилот: рычаги, предложения, эффект">{facts.loading?<section className="empty-state"><p>Загружаем факты…</p></section>:!facts.available?<section className="empty-state live-empty"><FileSpreadsheet size={30}/><h2>Нет фактов для сценария</h2><p>После импорта Excel в пилоте появится исходная экономика каждого доступного магазина.</p></section>:<><section className="page-lede"><div><h2>От гипотезы к контролируемому действию.</h2><p>Срез: <b>{rangeLabel}</b>. Выберите точку и готовый сценарий либо настройте рычаги вручную. Расчет — ориентир для пилота, не гарантированный финансовый результат: фиксируйте продажи в штуках, чеки и наличие товара.</p></div><div className="page-controls"><label>Магазин<select value={name} onChange={event=>setSelectedStore(event.target.value)}>{facts.storeNames.map(item=><option key={item} value={item}>{item}</option>)}</select></label></div></section><section className="scenario-strip"><div className="scenario-strip-head"><span>РЕКОМЕНДУЕМЫЕ СЦЕНАРИИ · {name}</span><small>Нажмите карточку — рычаги применятся к модели</small></div><div className="scenario-cards">{scenarios.map(scenario=><button key={scenario.id} onClick={()=>setScenario(scenario)} className="scenario-card"><span>{scenario.tag}</span><b>{scenario.title}</b><small>{scenario.text}</small><em>{scenario.why}</em></button>)}</div></section><section className="pilot-grid expanded"><article className="packet-card"><div className="card-title"><div><span>РЫЧАГИ СЦЕНАРИЯ</span><h3>Цена, объем, закупка, списания, расходы и запас</h3></div></div><Knob label="Цена товарного потока" value={price} onChange={setPrice} min={-10} max={10}/><Knob label="Объем продаж" value={volume} onChange={setVolume} min={-20} max={20}/><Knob label="Закупочная цена" value={purchase} onChange={setPurchase} min={-10} max={10}/><Knob label="Сокращение списаний М." value={writeoff} onChange={setWriteoff} min={-50} max={50}/><Knob label="Сокращение управляемых расходов" value={opex} onChange={setOpex} min={-15} max={15}/><Knob label="Изменение конечного остатка" value={stock} onChange={setStock} min={-30} max={30}/></article><article className="packet-card"><div className="card-title"><div><span>ЭФФЕКТ СЦЕНАРИЯ · {name}</span><h3>Что меняется в расчетной модели</h3></div></div><div className="effect-list"><div><span>Цена → прибыль</span><b className={model.priceProfit>=0?"positive":"negative"}>{model.priceProfit>=0?"+":""}{money(model.priceProfit)}</b></div><div><span>Объем → валовая прибыль</span><b className={model.volumeProfit>=0?"positive":"negative"}>{model.volumeProfit>=0?"+":""}{money(model.volumeProfit)}</b></div><div><span>Закупка → прибыль</span><b className={model.purchaseProfit>=0?"positive":"negative"}>{model.purchaseProfit>=0?"+":""}{money(model.purchaseProfit)}</b></div><div><span>Списания М. → прибыль</span><b className={model.writeoffProfit>=0?"positive":"negative"}>{model.writeoffProfit>=0?"+":""}{money(model.writeoffProfit)}</b></div><div><span>Расходы → прибыль</span><b className={model.opexProfit>=0?"positive":"negative"}>{model.opexProfit>=0?"+":""}{money(model.opexProfit)}</b></div><div><span>Высвобождение / заморозка денег</span><b className={model.cashRelease>=0?"positive":"negative"}>{model.cashRelease>=0?"+":""}{money(model.cashRelease)}</b></div></div><div className="scenario-result"><span>Оценочная чистая прибыль</span><strong>{money(model.profit)}</strong><small>маржа {formatPct(model.margin)} · покрытие {model.cover.toFixed(1)} дн.</small></div></article></section><section className="packet-card pilot-playbook"><div className="card-title"><div><span>КАК ЗАПУСТИТЬ ПИЛОТ</span><h3>Безопасная последовательность на 4 недели</h3></div></div><div className="playbook-grid"><div><b>1. Выберите один рычаг</b><span>Не смешивайте изменение цены со снижением списаний: иначе нельзя понять источник эффекта.</span></div><div><b>2. Зафиксируйте базу</b><span>Снимите продажи в штуках, выручку, валовую маржу, списания М. и остаток за 2–4 недели до запуска.</span></div><div><b>3. Ограничьте контур</b><span>Одна категория, один магазин, один ответственный. Сценарий цены проверяйте без изменения выкладки.</span></div><div><b>4. Примите решение</b><span>Масштабируйте только при подтвержденном росте валовой/чистой прибыли без ухудшения наличия и списаний.</span></div></div></section></>}</AuditShell>}
