import { useEffect, useMemo, useState } from "react";
import { CalendarDays, ChevronDown, RotateCcw } from "lucide-react";
import { endOfMonth, endOfQuarter, endOfWeek, format, parseISO, startOfMonth, startOfQuarter, startOfWeek, subMonths, subWeeks } from "date-fns";
import { ru } from "date-fns/locale";
import type { DateRange } from "react-day-picker";
import { Calendar } from "@/components/ui/calendar";
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
import { useAudit } from "@/contexts/AuditContext";
import "@/calendar-range.css";

const toIso=(date:Date)=>format(date,"yyyy-MM-dd");
type Shortcut={label:string;from:string;to:string;group:"Быстрые"|"Месяцы"|"Кварталы"};

export function DateRangeControl({compact=false}:{compact?:boolean}){const {range,setRange,rangeLabel}=useAudit();const [open,setOpen]=useState(false);const [showStandard,setShowStandard]=useState(false);const [draft,setDraft]=useState<DateRange>();const [hovered,setHovered]=useState<Date>();const selected:DateRange={from:parseISO(range.from),to:parseISO(range.to)};useEffect(()=>{if(!open){setDraft(undefined);setHovered(undefined)}},[open]);const applyRange=(next:{from:string;to:string},close=true)=>{setRange(next);setDraft({from:parseISO(next.from),to:parseISO(next.to)});if(close)window.setTimeout(()=>setOpen(false),260)};const selectRange=(next:DateRange|undefined)=>{setDraft(next);if(next?.from&&next?.to){applyRange({from:toIso(next.from),to:toIso(next.to)})}};const year=Number(range.from.slice(0,4));const shortcuts=useMemo<Shortcut[]>(()=>{const anchor=parseISO(range.from);const currentMonthStart=startOfMonth(anchor);const previousMonthStart=startOfMonth(subMonths(anchor,1));const currentWeekStart=startOfWeek(anchor,{weekStartsOn:1});const previousWeekStart=startOfWeek(subWeeks(anchor,1),{weekStartsOn:1});return [{group:"Быстрые",label:"Сегодня",from:toIso(new Date()),to:toIso(new Date())},{group:"Быстрые",label:"Текущая неделя",from:toIso(currentWeekStart),to:toIso(endOfWeek(anchor,{weekStartsOn:1}))},{group:"Быстрые",label:"Прошлая неделя",from:toIso(previousWeekStart),to:toIso(endOfWeek(subWeeks(anchor,1),{weekStartsOn:1}))},{group:"Быстрые",label:"Весь год",from:`${year}-01-01`,to:`${year}-12-31`},{group:"Месяцы",label:"Текущий месяц",from:toIso(currentMonthStart),to:toIso(endOfMonth(anchor))},{group:"Месяцы",label:"Предыдущий месяц",from:toIso(previousMonthStart),to:toIso(endOfMonth(previousMonthStart))},{group:"Кварталы",label:"I квартал",from:toIso(startOfQuarter(new Date(year,0,1))),to:toIso(endOfQuarter(new Date(year,0,1)))},{group:"Кварталы",label:"II квартал",from:toIso(startOfQuarter(new Date(year,3,1))),to:toIso(endOfQuarter(new Date(year,3,1)))},{group:"Кварталы",label:"III квартал",from:toIso(startOfQuarter(new Date(year,6,1))),to:toIso(endOfQuarter(new Date(year,6,1)))},{group:"Кварталы",label:"IV квартал",from:toIso(startOfQuarter(new Date(year,9,1))),to:toIso(endOfQuarter(new Date(year,9,1)))}]},[range.from,year]);const preview=draft?.from&&!draft.to&&hovered&&hovered>draft.from?{from:draft.from,to:hovered}:undefined;const label=draft?.from?(draft.to?`${format(draft.from,"d MMMM yyyy",{locale:ru})} — ${format(draft.to,"d MMMM yyyy",{locale:ru})}`:`Начало: ${format(draft.from,"d MMMM yyyy",{locale:ru})} · выберите конец`):rangeLabel;return <Popover open={open} onOpenChange={setOpen}><PopoverTrigger asChild><button className={compact?"date-range-control compact":"date-range-control"} aria-label="Изменить период анализа"><CalendarDays size={15}/><span>{rangeLabel}</span><ChevronDown size={13}/></button></PopoverTrigger><PopoverContent className="date-popover" align="end" sideOffset={10}><div className="date-popover-head"><span>ПЕРИОД АНАЛИЗА</span><b>{label}</b>{draft?.from&&<button type="button" className="date-reset" onClick={()=>{setDraft(undefined);setHovered(undefined)}}><RotateCcw size={13}/>Начать заново</button>}</div><Calendar mode="range" locale={ru} selected={draft} defaultMonth={draft?.from??selected.from} numberOfMonths={2} onSelect={selectRange} onDayMouseEnter={day=>setHovered(day)} onDayMouseLeave={()=>setHovered(undefined)} modifiers={preview?{preview}:undefined} modifiersClassNames={{preview:"range-preview"}}/><div className="date-shortcut-toggle"><button type="button" onClick={()=>setShowStandard(value=>!value)}>Стандартные периоды <ChevronDown size={13} className={showStandard?"rotated":""}/></button></div>{showStandard&&<div className="date-shortcuts">{(["Быстрые","Месяцы","Кварталы"] as const).map(group=><div key={group} className="date-shortcut-group"><small>{group}</small><div>{shortcuts.filter(shortcut=>shortcut.group===group).map(shortcut=><button type="button" key={shortcut.label} onClick={()=>applyRange(shortcut)}>{shortcut.label}</button>)}</div></div>)}</div>}<p className="date-popover-hint">Первый клик задает начало. Полоса предварительного диапазона появляется при наведении; второй клик применяет период и закрывает календарь.</p></PopoverContent></Popover>}
