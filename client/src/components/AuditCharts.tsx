/** Shared visual language for the CFO reporting packet: adaptive units and interactive series controls. */
import { useState } from "react";
import { Bar, BarChart, CartesianGrid, Cell, Line, LineChart, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { useAudit } from "@/contexts/AuditContext";

export const formatK=(value:number,digits?:number)=>{const absolute=Math.abs(value);if(absolute<0.01)return "0 ₽";if(absolute>=1000)return `${(value/1000).toLocaleString("ru-RU",{minimumFractionDigits:1,maximumFractionDigits:1})} млн ₽`;const precision=digits??(absolute<100?1:0);return `${value.toLocaleString("ru-RU",{minimumFractionDigits:precision,maximumFractionDigits:precision})} тыс. ₽`};
export const formatM=(value:number,digits=1)=>`${value.toLocaleString("ru-RU",{minimumFractionDigits:digits,maximumFractionDigits:digits})} млн ₽`;
export const formatPct=(value:number,digits=1)=>`${value.toFixed(digits)}%`;
type TooltipMode="amount"|"million"|"percent"|"number";
const chartTick=(value:number,mode:TooltipMode)=>mode==="percent"?`${value.toFixed(1)}%`:mode==="million"?`${value.toLocaleString("ru-RU",{maximumFractionDigits:1})} млн`:mode==="number"?`${value.toLocaleString("ru-RU",{maximumFractionDigits:1})}`:formatK(value).replace(" ₽","");
const iosPalette=["#0A84FF","#5E5CE6","#34C759","#FF9F0A","#FF375F","#64D2FF"];
const colors=(theme:"dark"|"light")=>theme==="light"?{grid:"#D7DDE5",axis:"#667085",tick:"#3F4A5A",dot:"#FFFFFF",median:"#FF375F",bar:"#5E5CE6",selected:"#0A84FF"}:{grid:"#3a2031",axis:"#b490a2",tick:"#d9c7d0",dot:"#130b12",median:"#ff597c",bar:"#762c4b",selected:"#ffc15e"};
export const sortTooltipPayload=<T extends {value:number}>(payload:T[])=>[...payload].sort((left,right)=>right.value-left.value);
export function TinyTooltip({active,payload,label,mode="amount"}:{active?:boolean;payload?:Array<{name:string;value:number;color:string}>;label?:string;mode?:TooltipMode}){if(!active||!payload?.length)return null;return <div className="tiny-tooltip"><b>{label}</b>{sortTooltipPayload(payload).map(item=><span key={item.name}><i style={{background:item.color}}/>{item.name}: <strong>{typeof item.value==="number"?chartTick(item.value,mode):item.value}</strong></span>)}</div>}
export function MetricLineChart({data,lines,percent=false,unit="k"}:{data:readonly Record<string,unknown>[];lines:{key:string;name:string;color:string}[];percent?:boolean;unit?:"k"|"m"}){const [hidden,setHidden]=useState<string[]>([]);const {theme}=useAudit();const palette=colors(theme);const mode:TooltipMode=percent?"percent":unit==="m"?"million":"amount";const toggle=(key:string)=>setHidden(current=>current.includes(key)?current.filter(value=>value!==key):[...current,key]);return <div className="metric-chart"><div className="chart-series-control" aria-label="Управление рядами графика">{lines.map((line,index)=>{const muted=hidden.includes(line.key),color=theme==="light"?iosPalette[index%iosPalette.length]:line.color;return <button type="button" aria-pressed={!muted} key={line.key} onClick={()=>toggle(line.key)} className={muted?"series-toggle muted":"series-toggle"}><i style={{background:color}}/>{line.name}</button>})}</div><ResponsiveContainer width="100%" height={306}><LineChart data={[...data]} margin={{top:18,right:32,left:20,bottom:10}}><CartesianGrid vertical={false} stroke={palette.grid}/><XAxis dataKey="month" axisLine={false} tickLine={false} stroke={palette.axis}/><YAxis axisLine={false} tickLine={false} width={78} stroke={palette.axis} tick={{fill:palette.tick,fontSize:11}} tickFormatter={value=>chartTick(value,mode)}/><Tooltip content={<TinyTooltip mode={mode}/>}/>{lines.filter(line=>!hidden.includes(line.key)).map((line,index)=>{const color=theme==="light"?iosPalette[index%iosPalette.length]:line.color;return <Line key={line.key} type="monotone" dataKey={line.key} name={line.name} stroke={color} strokeWidth={2.7} dot={{r:3.5,strokeWidth:2,fill:palette.dot}} activeDot={{r:6}} isAnimationActive animationDuration={720} animationEasing="ease-out"/>})}</LineChart></ResponsiveContainer></div>}
export function BenchmarkBars({data,median,mode="percent",selectedLabel}:{data:{store:string;value:number;selected:boolean}[];median:number;mode?:TooltipMode;selectedLabel:string}){const {theme}=useAudit();const palette=colors(theme);const height=Math.max(600,data.length*27+70);return <div className="benchmark-chart"><div className="median-badge"><i style={{background:palette.median}}/> Медиана сети: <b>{chartTick(median,mode)}</b> · {selectedLabel} — акцентный маркер</div><ResponsiveContainer width="100%" height={height}><BarChart data={data} layout="vertical" margin={{top:16,right:30,left:18,bottom:14}} barCategoryGap={6}><CartesianGrid horizontal={false} stroke={palette.grid}/><XAxis type="number" tickFormatter={value=>chartTick(value,mode)} axisLine={false} tickLine={false} stroke={palette.axis} tick={{fill:palette.tick,fontSize:11}}/><YAxis type="category" dataKey="store" width={128} tick={{fontSize:11,fill:palette.tick}} axisLine={false} tickLine={false}/><Tooltip cursor={false} content={<TinyTooltip mode={mode}/>}/><ReferenceLine x={median} stroke={palette.median} strokeWidth={2} strokeDasharray="5 5"/><Bar dataKey="value" name="Значение" radius={[0,6,6,0]} activeBar={false} isAnimationActive animationDuration={650}>{data.map(row=><Cell key={row.store} fill={row.selected?palette.selected:palette.bar}/>)}</Bar></BarChart></ResponsiveContainer></div>}
