# Проверка приёмки — 18.09.2026

В авторизованном dev-preview вручную проверены изменения прайс-контроля. Во вкладке «Связи» карточка показывает заголовок только имени связи, а входящие товары — отдельным вторичным списком; у первой связи отображалось три самостоятельных товара. Отметка группы выводит панель массовых действий «Скрыть выбранные» и «Снять выбор» без выполнения операции. В редакторе существующего товара видны раздельные поля «Имя связи» с внутренним поиском и «Новое имя связи», а код подписан как «Код товара».

В сравнении проверен двухэтапный сценарий: выбранное имя связи открывает товар этой связи, затем выбор товара определяет заголовок и историю предложений. В живой тёмной теме нет белых маркеров или вертикального hover-курсора на графике. Консоль браузера не показала ошибок. Скриншоты без авторизованной сессии показывают вход и не используются как оценка страниц прайс-контроля.

Проверка регулярной синхронизации: два Heartbeat-задания зарегистрированы и включены. Однако журнал первой задачи документов зафиксировал timeout, поэтому прежний пакетный запуск нельзя считать рабочим. Код изменён на работу с одним закреплённым складом на callback, с восьмисекундным внешним timeout и ротацией очереди; требуется публикация версии и проверка следующего запуска в production.


После преобразования строк остатков в карточки для ширины до 760 px в авторизованном desktop-preview подтверждены: данные-label присутствуют у всех девяти ячеек, действие «Изменить в пересчете» — flex-row с иконкой слева, горизонтального переполнения страницы нет. На широком экране таблица сохраняет drag-scroll как оправданная таблица девяти показателей; фактическая ширина таблицы 1622 px остается внутри прокручиваемого контейнера.


В авторизованном preview найдена незакрытая регрессия light-темы инвентаризации: карточка имеет тёмную границу `rgb(59,36,53)`, а primary-кнопка — тёмный фон `rgb(38,19,29)` при синем тексте. Это конфликтующий поздний override, а не принятый цветовой контракт; он будет удалён/переопределён в изолированных стилях перед приёмкой блока.


CSSOM trace for the affected inventory primary action found several competing late generic `.packet .packet-link` rules, including an unconditional dark `#26131d` background, after prior component-level rules. This confirms that the mismatch is a cascade conflict; the next edit will scope the operating actions after all generic layers rather than introduce a new color family.


После точечной коррекции в последнем тематическом слое повторная проверка light-темы инвентаризации подтвердила: у рабочей карточки фон `rgb(247,251,255)` и голубая граница `rgb(199,226,255)`, у primary-действия — ровный `rgb(10,132,255)` без градиента, белый контрастный текст и расположение иконки слева.


В live-preview каталога подтверждено, что отображаемый номер — последовательный бизнес-номер, а не PK. Одновременно найдено несоответствие: UI показывает 812 активных строк вместо подтвержденных 816, потому что запрос списка исключает архивные позиции. Это будет исправлено показом всех 816 строк с явной меткой и действием «Вернуть» для неактивной позиции, без удаления данных.


Повторная live-проверка после корректировки подтверждает полный общий каталог: **816 товаров**. В таблице отображается бизнес-номер («№ / ID / артикул») с последовательными значениями; внутренний PK не выводится. Четыре позиции с нераспознанной единицей не скрываются, а честно отмечены «Уточнить» до ручной корректировки карточки.


В live-preview склада после завершения запросов подтверждены **32** нескрытых склада, их назначения видов цен, печатной группы и статус read-only закрепления Эвотор без адресов/технических ID. Ручные кнопки чтения каталога и документов отсутствуют; в выбранной настройке отражены будущие автоматические интервалы 15/10 минут. Начальная «0 склада / загрузка» — обычное состояние до завершения пяти параллельных запросов, ошибок запросов не выявлено.


По новой приемке найдены и исправлены две визуальные регрессии. В «Выручке» поля суммы и сами input теперь имеют три различимых уровня light-поверхности (карточка → секция → поле) и отдельный глубокий input в dark-теме; focus остается только контуром. В dark-теме «Складов» outer-card, шапка и zebra-строки приведены к нейтральной палитре «Анализа магазинов» (`#16101a` / `#1c1420` / `#1b121c`) с коралловым контуром, вместо серо-синих поверхностей. Также удален вывод имени автора у строки выручки, поскольку фактическое значение могло содержать логин/телефон.


В dark live-preview «Выручки» проверена конечная иерархия редактора: нейтральная outer-card `#16101a`, секция `#110d14`, плитка суммы `#19131d`, ввод `#0e0a11` с контуром `#4b2c3c`. Поля больше не сливаются с фоном. В реестре автора больше не выводится: повторная проверка DOM подтвердила отсутствие ранее обнаруженного телефонного/логиноподобного значения.


При диагностике dark-стиля вручную менялся только атрибут HTML, что искусственно рассинхронизировало `data-audit-theme` и `data-pwa-theme`. Для корректной приемки состояние возвращено через `localStorage.audit-theme` и полную перезагрузку; последующие проверки проводятся только через штатный переключатель/новую загрузку.


После штатной перезагрузки в light-теме оба theme-атрибута синхронны. Форму «Выручки» проверено по вычисленным стилям: секция `#edf6ff`, плитка суммы `#f8fcff`, input `#fff` с голубым контуром `#9dcbed`; поля читаемо отделены от окружающих блоков без кораллового акцента.


В live-preview «Складов» редактор существующей группы печати содержит отдельные действия «Сохранить», «Скрыть», «Удалить», «Отмена». Удаление требует явного браузерного подтверждения и на сервере отсоединяет только склады; товар, цена, документ и история печати не затрагиваются. Действие не выполнялось в приемке, чтобы не менять пользовательскую конфигурацию.


В live-preview карточки товара подтверждены: «К списку» — обычная ссылка с иконкой слева; НДС — компактное поле; штрихкоды — одно широкое поле, без горизонтального выхода; при выборе «Маркированное пиво» появляются алкокод, controlled-select кода вида АП (ФСРАР), крепость и объем тары. Добавлен read-only вывод документированного кода типа товара Эвотор: `NORMAL` или `ALCOHOL_MARKED`; это информационное соответствие для будущего согласованного контракта, не операция записи в Эвотор. В live после HMR для текущего товара БАД отображается `NORMAL`.


Для безопасной проверки login без выхода из авторизованного dev-preview открыт публичный домен. В этот момент страница вернула только title без DOM, поэтому визуальная проверка на опубликованном домене не засчитывается и изменение сессии в рабочем preview не выполнялось. Контракт входа подтвержден исходным кодом и 6 целевыми тестами; полноценную live-проверку login следует повторить после доступности public DOM либо в отдельной неавторизованной сессии.


Изолированный mobile-capture 375×812 открывает неавторизованный экран входа для всех запрошенных маршрутов, что подтвердило фактический iOS-подобный переключатель входа для магазинов в dark-теме: pill 38×22, круглый thumb, без видимой browser-checkbox. Иконка темы в dark имеет желтый цвет. Проверка light-значения остается контрактной (`#20202a`), так как screenshot-service начинает новую неавторизованную dark-сессию для каждого маршрута.


Live desktop inspection of `/stock-control` exposed the remaining light-table issue: although the outer working card was white, the stock table used `#f7fbff` for odd rows and `#edf6ff` for even rows, so a large table looked solid blue. The next correction will retain iOS-blue as a card/contour accent but make the tabular body a near-white zebra, consistent with «Ритм». At this inspection, page-level horizontal overflow was zero.


After the table correction, live light `/stock-control` styles are: outer card `#f7fbff`, table frame white, odd row white, even row `#fafcff`, header `#f2f8ff`, and page overflow 0 px. This retains the requested iOS-blue hierarchy outside the data body while restoring the near-white zebra rhythm.


Live dark `/stock-control` inspection confirms the shared neutral/coral hierarchy: outer card and odd rows `#16101a`, header and even rows `#1c1420`, with no peach fill or moving-row hover. Page overflow remains 0 px.


Live dark `/inventory-control` inspection confirms zero page overflow and the requested neutral/coral hierarchy: new-counting card `#16101a`, history row `#17111b`, and the main action `#26131d` with coral contour/text, without a peach fill or secondary square hover tile.


Live light `/inventory-control` inspection confirms zero page overflow: create card `#f7fbff`, history row white, and primary action iOS-blue `#0a84ff` with white text. The visual system therefore has the same blue/near-white hierarchy as the corrected stock screen.


Live light `/catalog-control` inspection exposed the same overly-blue striped data body and then confirmed the correction: outer card `#f7fbff`, white table frame and odd rows, `#fafcff` even rows, `#f2f8ff` header, and 0 px page overflow. The business catalog count remained 816; visible identifiers are sequential business numbers rather than database primary keys.


The two new admin-only Evotor sales routes were opened in the authenticated preview. Their navigation appears under «Управление магазинами»; the store chooser is collapsed by default, has 0 px horizontal overflow, and the empty state explicitly states that it waits only for automatic read-only document loading. The dark product page uses the existing neutral/coral contour pattern, with no added palette. At this moment there are no normalized receipts in the selected 2026 range, so the graph/table’s populated state cannot honestly be visually accepted until the production scheduler executes successfully.


Live desktop acceptance of `/catalog-control` confirmed the general catalog still contains 816 items, the visible business number begins from 1, and the technical primary key is not printed. The table’s edit control is now a 32px icon-only semantic link: it has no visual text, retains the full accessible label and tooltip, and the page has 0 px horizontal overflow outside of its dedicated table wrapper.


Live acceptance of the warehouse list after the surface correction confirmed 32 visible warehouses. In the dark theme the list card is `#16101a` and table heading `#1c1420`; after the standard theme toggle, the light page has the blue operational hierarchy while table rows remain near-white. The page has 0 px horizontal overflow outside its dedicated table structure. Open settings and request-print category cards now use the same warehouse surface tokens instead of generic grey panels.


The isolated 375×812 screenshot runner was used for eight operating and price routes. It correctly reaches the compact login surface for each route but does not share the authenticated browser session, so it cannot be used to falsely claim visual acceptance of protected operating data. The compact login itself fits within the viewport with the iOS-like store switch and left-aligned icons. Protected-page mobile behavior remains covered by the explicit per-page responsive CSS/contract suite; final visual acceptance of authenticated tables must use a real signed-in narrow device session.


The browser diagnostic view after the current dashboard and warehouse changes contains no console errors. The previous Vite transform messages were historical development output; current TypeScript verification and the live browser session are clean.


В рамках приёмки компоновки прайс-контроля и «Управления магазинами» выровнены базовые поверхности интерактивных полей. На светлой теме рабочая карточка использует `#f7fbff` с контуром `#c7e2ff`, вложенные input/select — белую поверхность с тем же голубым контуром; в темной теме рабочая карточка — `#16101a`, вложенные input/select — `#110d14`, а контур — `#3b2435`. Коралловый `#ff765f` оставлен только для выбора, фокуса и hover, не как заливка таблиц или карточек.

В живом desktop-preview на `/warehouse-control` подтверждены 32 склада, отсутствие горизонтального переполнения, активный mouse-drag на таблице и корректная тематическая иерархия. В light вычисленные значения: карточка и обертка таблицы `#f7fbff`, поля/селекты белые, все границы `#c7e2ff`; в dark: карточка/таблица `#16101a`, поля/селекты `#110d14`, границы `#3b2435`. В живом `/price-control` подтверждено отсутствие горизонтального переполнения и та же иерархия для основного блока сравнения.

Для ширины 761–1120 px строки сохраненного preview прайса перестроены: название занимает независимую широкую левую область, а цены и связь — правую. До 760 px каждая строка становится вертикальной карточкой, поэтому длинное название, селекты и кнопки не сдавливают друг друга и не требуют боковой прокрутки. Основные подтверждающие действия могут занимать полную строку только на узком экране; вторичные controls остаются компактными.


## Ревизия и соответствие типов Эвотор V2

В авторизованном preview открыта существующая ревизия ПОРТ. После исправления компоновки у черновика нет горизонтального выхода: `scrollWidth − clientWidth = 0`. Форма добавления товара на среднешироком экране занимает две равные колонки, а не сжимает действия. Карточка истории имеет `overflow: hidden`; контур hover рисуется inset и не выходит наружу блока. В верхней части открытого черновика расположены «Сохранить», «Заполнить учетными остатками» и сортировка. Для каждой добавленной строки предусмотрено редактируемое поле фактического остатка с единицей измерения, немедленный пересчет разницы и итогов по единицам; верхняя «Сохранить» передает только действительно измененные строки в ограниченных параллельных пакетах. Тестовое заполнение настоящего черновика не запускалось: оно сохраняет реальные строки, а разрешения на запись тестовых операционных данных не было.

В карточке номенклатуры проверены выбор «Маркированное пиво», тип `BEER_MARKED`, видимые дополнительные поля и селект кода вида АП с вариантами `500` и `510`. Типы Эвотор теперь соответствуют официальной V2-справке: БАД — `DIETARY_SUPPLEMENTS_MARKED`, икра — `CAVIAR_MARKED`, консервы — `GROCERIES_MARKED`, маркированное пиво — `BEER_MARKED`, безалкогольное пиво — `NOT_ALCOHOL_BEER_MARKED`; `NORMAL` строго остается без маркировки. Обратная запись в Эвотор не создавалась.


## Прайс‑контроль: приемка preview и связей

В авторизованном dev-preview открыт сохраненный прайс «МОРОЖКА цены с 14.09.2026.pdf». У строки № 1 отображается код и имя связи в порядке `A419 · Тримминг (кожа, плавники), блок`. После «Изменить» действие меняется на «Отменить», отдельный внешний поиск связи отсутствует, а поиск остается внутри раскрываемого списка. В dropdown проверены **472** доступных имени связи: каждое выводится как `код · имя`, начиная с `A059 · …`; это устраняет прежнее ограничение первых 40 вариантов.

В редакторе строки действия выровнены сеткой, а «Готово» занимает отдельную полную строку. На узкой раскладке имя связи больше не сужается до узкой колонки: оно занимает доступную ширину ниже заголовка и цены. Tooltip истории цен ограничен шириной 346 px на обычной компактной ширине и 306 px на телефоне, не выходит за viewport.

Во вкладке «Связи» у каждого входящего товара есть отдельное действие «Убрать из связи». Оно снимает только принадлежность к группе сравнения; товар, предложение и история цен сохраняются. Вызов защищен правом `edit` и фиксируется через общий `recordChange`; действие не выполнялось в приемке, чтобы не менять пользовательские данные.

Скриншот‑раннер на 375 px не использовался как приемка защищенного содержимого, поскольку он открывает отдельную неавторизованную сессию. Подтверждена компоновка в авторизованном браузере и добавлены контрактные проверки адаптивных CSS‑правил.


## Автоматическая синхронизация Эвотор: production-приемка

Проверены platform-managed Heartbeat задания. В production включены два независимых read-only callback: каталог и остатки — каждые 15 минут, документы и товарные строки — каждые 10 минут. Последний проверенный запуск каталога завершился HTTP 200 за 10,66 с и прочитал 815 товаров одной закрепленной точки; два последних запуска документов завершились HTTP 200 за 7,28 с и 8,71 с, каждый прочитал 1 000 чеков и соответственно 1 466/1 592 строки. Ранние timeout относятся к версии до ограничения callback одной точкой и пакетной записи. Токены, адреса, terminal UUID и любые write-вызовы в Эвотор отсутствуют.


## Адаптивность операционных экранов

Сверены правила средней и узкой ширины. Остатки и номенклатура переходят из data-table в подписанные карточки до 1200 px, склад — до 1400 px, строки ревизии — до 1024 px; при этом действия остаются в строке с иконкой слева. Сохраненный preview прайса на 761–1120 px отдает независимую широкую область названию, а на телефоне строит вертикальную карточку без бокового drag-scroll. Новые страницы продаж Эвотор теперь переводят таблицу графика в подписанные карточки уже до 1024 px, а не только на телефоне. Контрактный набор шести операционных экранов прошел: 105 проверок; отдельная авторизованная mobile-сессия по-прежнему недоступна screenshot-runner, поэтому это не выдается за live-приемку данных на телефоне.


## Заявки магазинов и аналитика Эвотор

В авторизованном dev-preview открыт новый маршрут «Заявки». Экран показывает отдельный черновик выбранного нескрытого склада, историю этого склада и административную подготовку печати по группам магазинов и категориям. В запросе к странице не выводятся себестоимость, внутренние цены, адреса или технические идентификаторы. База подтверждена: миграция `0045_old_blackheart.sql` применена, таблицы заявок и строк существуют, а у категорий печати есть режим `per_store` / `grouped_stores`.

Проверен read-only объем фактической витрины Эвотор: сохранено **31 395** нормализованных документов и связанные строки, с доступным диапазоном **10.06.2018—18.07.2022**. Поэтому новые страницы «Показатели Эвотор» и «Проданные товары» находятся в «Анализе магазинов» и при первом входе безопасно переключаются на последние 30 дней реально загруженной витрины. Они не показывают фиктивные нули для активного финансового диапазона 2026 года. Оба экрана сохраняют ручной выбор периода через общий календарь, общий/выбранный набор магазинов, месяц/неделю/день/время, отдельный ряд магазина, график и таблицу под ним.

В «Прогнозе» добавлен отдельный read-only сигнал чеков для того же финансового среза. При несовпадении периодов он показывает доступность витрины, но не переносит старые чеки в финансовый прогноз и не смешивает их с P&L.


## Заявки, адаптивность и сохранение границы 2025

В авторизованном live-preview проверена компоновка страницы «Заявки»: ширина карточек не превышает ширину рабочего полотна, а форма открытия черновика и печатная конфигурация не создают горизонтального переполнения. Создание заявки перестроено в сценарий продавца: его точка фиксируется, дата — сегодняшний московский день; администратор может выбрать доступную точку и дату. В открытом черновике номенклатура раскрывается по категориям, количество меняется прямо в строке кнопками `−`/`+` или числом с нормализацией запятой в точку. Для видимой позиции показываются только неценовые операционные сигналы: качественный уровень остатка, продажи за семь дней, ориентир дней запаса и предупреждение о чрезмерном объеме. Скрытые для заявок товары доступны исключительно администратору; если позиции нет в общем каталоге, она может быть добавлена только строкой текущей заявки без создания номенклатуры.

Для компактных размеров реестр «Выручки» перестраивается в подписанные карточки на ширине до 920 px. Номенклатура и склад получили подписи действий и ограничение полей/селекторов внутри карточек настройки; это убирает обрезание текста и выход редактора склада за контейнер. Действия заявки используют общий нейтральный контур кнопок без отдельной оранжевой заливки. В аналитике Эвотор цвета линий ограничены согласованными акцентами: iOS-синий/зелёный/жёлтый/красный в светлой теме и коралл/зелёный/жёлтый/приглушённый коралл в тёмной.

Синхронизация документов продолжает принимать только нормализованные факты от `01.01.2025`; старые ранее сохраненные документы физически не удалялись и не участвуют в новой аналитике. Проверенный production-журнал Heartbeat показывает успешные read-only запуски каталога каждые 15 минут и документов каждые 10 минут. Удаление сохраненных до 2025 года данных не выполнялось, поскольку это необратимая операция и не требуется для исключения их из новых импортов и аналитических экранов.


## Компактный desktop: операционные таблицы и редактор склада

При ширине **1280 px** в авторизованном preview проверены «Остатки», «Выручка», «Инвентаризации» и «Склады». Для таблиц, в которых перенос строк полезнее горизонтальной прокрутки, рабочие записи переходят в подписанные карточки: ширина документа не выходит за viewport, а drag-scroll при этом режиме отключен. В «Остатках» действие «Изменить в пересчёте» сохраняет обычную высоту и контур общего паттерна, без нового оранжевого оформления.

Отдельно воспроизведено и исправлено переполнение при нажатии «Изменить» в складе: раскрытый редактор ранее превышал карточку на 56 px. Для режима до 1400 px таблица, строка настроек и её поля теперь жёстко ограничены шириной карточки (`width/max-width: 100%`, `min-width: 0`); после исправления проверка DOM показывает **0 px** горизонтального переполнения, при ширине карточки 1006 px редактор занимает 952 px. Поля и действия сохраняют одну колонку без прижатия к краю.


## Заявки и витрины Эвотор: проверка 19.09.2026

В авторизованном preview повторно открыта существующая незакрытая заявка. Повторное действие «Открыть заявку» не создало второй черновик и показало уведомление о возврате в существующий. В открытом черновике видны только «Распечатать и закрыть» и «Удалить черновик»: отдельного действия «Закрыть заявку» нет. Первое действие доступно только руководителю/администратору и только для непустой заявки; удаление оставлено отдельным полезным действием. Поиск раскрывает совпадающие категории только при входе в режим поиска: после этого пользователь может снова свернуть любую категорию.

Общее поле комментария удалено из UI и tRPC-маршрута. Остаются ровно два независимых комментария, каждый с выбором категории печати. Ручная строка «Не нашли товар?» не создает карточку номенклатуры: сервер требует активную категорию печати и сохраняет только строку конкретного черновика. В печатной проекции такая строка попадает в выбранную категорию, а обычные строки — по категории номенклатуры; печать по-прежнему включает все активные группы без выбора пользователем. Миграция `0050_old_union_jack.sql` применена: в `operational_store_request_lines` имеются nullable-поля `manualProductName` и `manualPrintCategoryGroupId`.

Две независимые страницы в «Анализе магазинов» теперь используют структуру «Ритма»: общий/выбранные магазины, месяц/неделя/день/время, режим графика, показатели над графиком и таблица под ним. На странице товаров выбираются сами товарные позиции для линий, а не только общий агрегат; таблица ниже показывает количество, единицу (`fraction` визуально как «кг»), сумму, строки чеков и магазины. В серверной агрегации учитывается только `SELL`; витрина остается server-side read-only и не является финансовым P&L.

Фактическое покрытие проверено отдельным read-only запросом БД: в сохраненном окне с 01.01.2025 есть продажи у двух видимых точек. На момент проверки: «МИРА» — 25 748 чеков (04.01.2025—18.09.2026), «ПЗ2» — 2 786 чеков (04.01.2025—25.02.2025). Поэтому интерфейс явно сообщает число точек с загруженными фактами и не выдает срез «Все магазины» за полный backfill сети.

19.09.2026 повторно проверено managed Heartbeat: оба включенных callback имеют cron `0 * * * * *` — одну минуту. Последние журналы подтверждают HTTP 200: документы обрабатывают одну точку за 2,8—7,4 с и до 1 000 чеков/1 513 товарных строк за callback; каталог обрабатывает одну точку за 10,5—12,8 с и 815 товаров. Это не означает поминутное обновление каждой точки: очередь последовательно обслуживает один закрепленный склад за callback. В Эвотор не выполняется обратная запись.

Полная проверка исходного набора успешна: `pnpm test` — 112 файлов / 602 теста; `pnpm check`, `pnpm build` и `git diff --check` — успешно. В browser DOM для обоих заполненных экранов Эвотор подтверждены таблица и график, 0 px горизонтального переполнения desktop. Screenshot-runner не использовался для оценки защищенного содержимого, поскольку создает отдельную неавторизованную сессию.

## Приёмочный блок 19.09.2026: печатные категории, рекомендации и витрина Эвотор

В БД применены миграции `0051_overconfident_aqueduct.sql`, `0052_aspiring_nemesis.sql` и `0053_tidy_arachne.sql`. Категория печати теперь хранит группу складов-источников для качественного сигнала доступности, слот комментария и ограничение запаса магазина в днях. В редакторе «Складов» этот лимит показан как контролируемое поле «Макс. запас в магазине, дни» со значением по умолчанию 2; в live DOM редактор открылся без горизонтального переполнения. Выбор вида цены, группы печати и закрепления Эвотор у склада сохраняется сразу при выборе и больше не требует отдельной кнопки. Точные остатки склада-источника в заявке не показываются: доступность обозначается качественным статусом, а рекомендация использует средние продажи за 7 дней, текущий остаток магазина и норму категории. Если запас превышает заданную норму покрытия, рекомендация честно предлагает не заказывать товар в этот день.

Для печати заявок добавлено групповое «Распечатать и закрыть»: действие закрывает все непустые открытые заявки выбранной даты, для которых пользователь имеет административное право, и стоит рядом с обычной печатью, а не внутри одной заявки. Обычного отдельного действия закрытия нет. Скрытые магазины отфильтрованы на сервере как из проекции печати заявок, так и из реестра/печати выручки; клиентский список также не предлагает их для выбора. Печатная категория хранит назначение одного из двух комментариев, поэтому это назначение не зависит от конкретного черновика.

В авторизованном desktop-preview на ширине 1280 px подтверждены: раскрытый черновик инвентаризации, строка истории и таблица пересчёта имеют `0 px` горизонтального переполнения; поиск добавляемого товара находится внутри searchable select, отдельного поля поиска нет. В `light` кнопка «Открыть пересчёт» имеет белый фон, голубой контур `rgb(189, 217, 246)`, читаемый текст `rgb(7, 93, 187)` и не имеет тени. В `dark` та же кнопка использует нейтральный фон `rgb(27, 20, 28)`, приглушённый контур `rgb(93, 53, 71)` и также не имеет тени; коралл появляется только на hover/focus. Вложенная квадратная подсветка строки истории отключена в обеих темах, а контур остаётся на карточке-родителе.

Витрина «Показатели Эвотор» после оптимизации индексами и ограниченного начального периода загрузилась в live browser без ошибки: текущий диапазон витрины `03.01.2025—18.09.2026`, для активного 30-дневного среза есть факты у **7 из 32** нескрытых выбранных магазинов, отображаются 9 723 чека и таблица из 5 интервалов; page overflow равен 0 px. Неполный backfill не скрывается: интерфейс прямо показывает счётчик `7 из 32` и не утверждает, что каждая точка обновляется каждую минуту. Оба пути витрины остаются server-side read-only: новый вызов Эвотор и обратная запись отсутствуют.

Проверено после изменений: `pnpm test` — **112 файлов / 606 тестов**, `pnpm check`, `pnpm build`, `git diff --check` — успешно. Неавторизованный screenshot-runner снова не использовался как доказательство мобильной приёмки защищённых данных; для мобильного состояния сохранены CSS и контрактные проверки, а live-проверка выполнена в авторизованном desktop browser.


## Приёмочный блок 19.09.2026: категории, печать и сверка выручки

Введён управляемый справочник категорий номенклатуры. Миграция `0054_sour_red_ghost.sql` добавляет стабильные ссылки категории в карточку товара, строку заявки и состав печатной категории; перед фиксацией проверена фактическая обратная совместимость: в справочнике создано **28** категорий, связаны все **816** позиций, **27** существующих членов печатных подборок и **3** существующие строки заявок. Редактор товара использует этот справочник без создания товаров в других разделах; в каталоге для администратора доступна отдельная работа с категориями.

Настройки печати собраны на отдельной странице «Настройки печати». Там в одном месте находятся группы магазинов, категории печати, вложенные категории, режим отдельного листа или общего листа, источник качественного индикатора остатка, максимальное покрытие в днях и назначение одного из двух слотов комментария. Привязка склада к группе и точки Эвотор в редакторе склада сохраняется непосредственно при выборе; отдельные неразличимые кнопки сохранения для этих назначений удалены. Скрытые магазины исключены из реестра выручки и из проекции печати заявок на сервере.

Карточка товара переработана в компактную сетку: бизнес-номер выводится как короткая метка, штрихкоды и алкокод используют вертикально растягиваемые многострочные поля. На сенсорных устройствах кнопка разворачивания находится внутри поля, а на устройствах с указателем остаётся нативное вертикальное растягивание, как в согласованном контроле прайс-листа. Код `fraction` остаётся внутренним, а отображается как «кг».

Печатные стили заявок и реестра «Выручка» дополнительно ограничены отдельным белым листом A4 portrait с полями **4 мм**: ячейки и фон белые, отступы таблиц уменьшены для размещения в пределах листа. Визуальная физическая приёмка вывода конкретного принтера не выполнялась; проверены отдельный print DOM и CSS-правила.

В реестре «Выручка» добавлена новая read-only таблица «Сверка с чеками Эвотор». Она сопоставляет только переданные **Нал + Б/Нал** с суммой нормализованных документов `SELL`, не включает расходы и не меняет P&L, записи реестра или Эвотор. Отсутствие импортированных чеков и отсутствие передачи отражаются отдельными статусами, а не нулями. Доступ ограничен административными ролями и назначенными точками тем же серверным контуром, что и реестр. В live-preview блок отрисовывается без горизонтального переполнения; для выбранной на момент проверки даты сопоставимых фактов нет, поэтому показано честное пустое состояние.

Проверено: `pnpm test` — **112 файлов / 606 тестов**; `pnpm check`; `pnpm build`; `git diff --check`. В Эвотор не добавлялись write-вызовы, финансовые факты и исторический Excel-импорт не изменялись.


## Приёмочный блок 19.09.2026: заявки, настройки печати и реестр

В интерфейсе заявок удалено создание request-only товара: строка заявки формируется только из общего справочника. Для открытого черновика добавлено явное действие «Отмена», а удаление доступно также непосредственно в списке черновиков. Руководитель/администратор получает переключатель «Все магазины» в реестре заявок; при единственном выбранном магазине доступна только «Распечатать», при всех доступных магазинах появляется «Распечатать и закрыть». Сервер проверяет область назначенных точек и не позволяет передать произвольную точку в печать. Продавец остается ограничен своей точкой и текущей датой.

Настройки печати теперь содержат видимые строчные контролы для групп магазинов и плотную двухколоночную раскладку подборок, которая переходит в один столбец до переполнения. Проверка в авторизованном браузере на ширине 1280 px: у страницы настроек, блока подборок и всего документа переполнение равно 0 px. Комментарии привязаны к настройкам категорий и в пользовательских формах названы «Комментарий к Мороженной продукции» и «Комментарий к Копченой продукции»; их места печати по-прежнему задаются в настройках категории, а не в заявке.

Справочник категорий номенклатуры перестроен из плиток в плотный список строк; действие «Архивировать» переименовано в «Скрыть». В авторизованном браузере на 1280 px список из 28 категорий и документ не имеют горизонтального переполнения. В реестре «Выручка» удалена пользовательская секция «Сверка с чеками Эвотор»: факты Эвотор остаются в «Ритме», а операционный контур выручки не дублирует этот анализ. В диалоге отмены закреплен интерактивный contour-hover в обеих темах.

Полная техническая проверка: `pnpm test` — 112 файлов / 605 тестов; `pnpm check`; `pnpm build`; `git diff --check` — успешно. Печатный лист, финансовые права, historic Excel и любые write-вызовы в Эвотор в этом блоке не изменялись.


## Приёмочный блок 19.09.2026: чеки Эвотор в «Ритме» и приоритет сегодня

В «Ритм» встроены пять read-only фактов чеков: **Выручка общая Эвотор**, **Выручка нал Эвотор**, **Выручка б/нал Эвотор**, **Чеки Эвотор** и **Средний чек Эвотор**. Новый блок в финансовом реестре не создавался: финансовая передача и права не менялись. Для оплат хранятся только агрегированные суммы типов `CASH`, `ELECTRON`, прочих и неизвестных и статус сверки суммы; реквизиты, части платежа, сдача, идентификаторы и любые данные платежного средства не сохраняются.

Страница «Проданные товары» переписана на контролы «Ритма»: месяц/неделя/день/время, общий либо выбранные магазины, совместный или построчный режим магазинов для одного товара, выбранные товары в цветных чипах графика и одна таблица непосредственно под ним. Убраны поле поиска, массовое «Выбрать все», внутренний скролл selector, отдельная таблица состава и показатель «Товарные позиции». В live browser-preview загрузка завершилась без ошибки: отрисованы недельный график и единственная таблица с теми же интервалами и выбранным товаром.

Миграции `0055_dizzy_wonder_man.sql` и `0056_old_bastion.sql` применены: добавлены агрегаты оплат и независимый режим cursor-серии `current_day`. Платформенный Heartbeat уже использует минимальный допустимый cron `0 * * * * *`; его частота не увеличивалась. В новом исходнике суточное окно проходит все 32 закреплённые нескрытые точки по одной cursor-странице, затем возобновляется backfill с 01.01.2025. До новой публикации production продолжает исполнять предыдущую опубликованную версию callback; опубликованный cron уже минутный. В аудит callback добавляется только числовое наблюдение `X-RateLimit-*`, если заголовки API отданы.

Подтверждено на момент проверки: 32 из 32 видимых точек имеют mapping; документы 2025+ есть у 8 точек. Поэтому нельзя утверждать, что архив или сегодняшний срез уже полны. Полный regression suite: **112 файлов / 609 тестов**; `pnpm check`, `pnpm build` и `git diff --check` успешны.
